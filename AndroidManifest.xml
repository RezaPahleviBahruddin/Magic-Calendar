<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="magsoft.magic_calendar"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="magsoft.magic_calendar.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="magsoft.magic_calendar.AddReminderActivity"
            android:label="@string/add_reminder"
            android:parentActivityName="magsoft.magic_calendar.MainActivity" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity 
            android:name = ""
            ></activity>
        <activity
            android:name="magsoft.magic_calendar.ControlCalendarActivity"
            android:label="@string/title_activity_control_calendar" >
        </activity>
    </application>

</manifest>


